<div class="container">
  <div class="col-sm-12">
    <h1 class="text-center heading"><%= @user.name %></h1>
  </div>
  <div class="col-sm-4">
      <h3>Your Analytics</h3>

      <br>
      <div class="col-xs-6">
        <p>You've attended...</p>
        <p class="text-center prominent-number"><%= @user.events_attended %></p>
        <p class="text-center"><small>event(s)</small></p>
      </div>

      <div class="col-xs-6">
        <p>You've hosted...</p>
        <p class="text-center prominent-number"><%= @user.events_hosted %></p>
        <p class="text-center"><small>event(s)</small></p>
      </div>

      <hr class="col-xs-10 col-xs-offset-1">

      <h5 class="text-center">You order an average of <span class="double-size"><%= @user.avg_num_slices_bought %></span> slices</h5>

      <!-- %= pie_chart @user.slices_consumed_by_type %> -->

      <hr>
      <em><h5 class="text-center"><%= @user.total_consumed %></h5></em>
      <hr>

      <div class="col-xs-6">
        <p>You've consumed...</p>
        <p class="text-center prominent-number"><%= @user.calories_consumed %></p>
        <p class="text-center"><small>calories</small></p>
      </div>

      <div class="col-xs-6">
        <p>You'd need to run...</p>
        <p class="text-center prominent-number"><%= @user.miles_needed_to_run %></p>
        <p class="text-center"><small>miles</small></p>
      </div>
  </div>

  <div class="col-sm-6">

    <h2>Active Orders </h2>
    <ul class="list-group">
      <em><%= "No current orders. Get in on some pizza!" if @active_orders.empty? %></em>
      <% @active_orders.each do |order| %>
        <%= link_to event_order_path(order.event, order), class: "list-group-item list-format" do  %>
        <div class="row">
          <div class="col-xs-2">
            <h4 class="text-center"><%= order.event.date.strftime("%b %e") %></h4>
            <p class="text-center"><%= order.event.date.strftime("%l:%M %P") %></p>
          </div>
          <div class="col-xs-8">
            <h5><%= order.event.title%></h5>
            <p class="list-group-item-text">  <%= order.event.description%></p>
            <em><p class="list-group-item-text">  Ordering closes <%= order.event.deadline.strftime("%A, %B %e at %l:%M %P") %></p></em>
          </div>
          <div class="col-xs-2">
            <p class="text-right prominent-number"><%= order.slices.count %></p>
            <p class="text-right"><small>slices</small></p>
          </div>
        </div>
        <%end%>
      <%end%>
    </ul>

    <h4>Previous Orders </h4>
    <ul class="list-group">
      <em><%= "No current orders. Get in on some pizza!" if @active_orders.empty? %></em>
      <% @inactive_orders.each do |order| %>
        <%= link_to event_order_path(order.event, order), class: "list-group-item list-format" do  %>
        <div class="row">
          <div class="col-xs-2">
            <h4 class="text-center"><%= order.event.date.strftime("%b %e") %></h4>
            <p class="text-center"><%= order.event.date.strftime("%l:%M %P") %></p>
          </div>
          <div class="col-xs-8">
            <h5><%= order.event.title%></h5>
            <p class="list-group-item-text">  <%= order.event.description%></p>
            <em><p class="list-group-item-text">  Ordering closes <%= order.event.deadline.strftime("%A, %B %e at %l:%M %P") %></p></em>
          </div>
          <div class="col-xs-2">
            <p class="text-right prominent-number"><%= order.slices.count %></p>
            <p class="text-right"><small>slices</small></p>
          </div>
        </div>
        <%end%>
      <%end%>
    </ul>
  </div>

</div>
